const config = {
  token: (process.env.TOKEN),

  // Settings
  defaultSettings: {
    prefix: ';;', // this again can be changed to whatever prefix you would like.
  },
  };
  
  module.exports = config;